<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.demo.authentication.domain.repository.UserRepository">

    <resultMap id="userResultMap" type="com.example.demo.authentication.domain.User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="isActive" column="is_active"/>
        <collection property="roles" ofType="com.example.demo.authentication.domain.Role">
            <id property="id" column="role_id"/>
            <result property="name" column="role_name"/>
        </collection>
    </resultMap>

    <select id="findByEmail" resultMap="userResultMap">
        SELECT
            u.id,
            u.name,
            u.email,
            u.password,
            u.is_active,
            r.id as role_id,
            r.name as role_name
        FROM
            user_master u
        LEFT JOIN
            user_role ur ON u.id = ur.user_id
        LEFT JOIN
            role_definition r ON ur.role_id = r.id
        WHERE
            u.email = #{email}
    </select>

</mapper>